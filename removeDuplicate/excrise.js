const numbers = [1, 2, 3, 4, 5];
const searchNumber = 5;

if (numbers.includes(searchNumber)) {
  console.log(`${numbers} includes ${searchNumber}`);
} else {
  console.log(`${numbers} does not include ${searchNumber}`);
}
